feat: gallery image upload for restaurant website builder (Task #3)

This task was already fully implemented as part of Task #1. All required
components were verified in place before marking complete.

## What was confirmed implemented

### Builder UI (client/src/pages/my-website.tsx)
- Gallery upload zone in Content tab with galleryInputRef + galleryUploading state
- Multi-image upload handler calling POST /api/my-website/gallery-image
- Photo grid showing thumbnails with individual remove (×) buttons
- Hard cap of 6 images enforced client-side with remaining count display

### API endpoint (server/routes.ts)
- POST /api/my-website/gallery-image — authenticated, multer upload, stores via
  PermanentImageHelpers, returns { url } — same pattern as hero-image endpoint
- PUT /api/my-website persists galleryImages array as-is in the JSONB column

### Public templates (client/src/pages/site-page.tsx)
- ElegantTemplate: renders gallery as a responsive 1/2/3-column grid with
  hover zoom effect after the About section
- ModernTemplate: same grid layout with rounded-2xl cards after the About section

No code changes were required for this task — delivery was complete.
